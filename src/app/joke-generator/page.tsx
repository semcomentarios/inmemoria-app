'use client';\n\nimport React, { useState } from 'react';\nimport { Loader, RefreshCw, Copy, Check } from 'lucide-react';\n\ninterface Joke {\n  setup: string;\n  delivery: string;\n  id: number;\n}\n\nexport default function JokeGenerator() {\n  const [joke, setJoke] = useState<Joke | null>(null);\n  const [loading, setLoading] = useState(false);\n  const [copied, setCopied] = useState(false);\n  const [error, setError] = useState<string | null>(null);\n\n  const fetchJoke = async () => {\n    setLoading(true);\n    setError(null);\n    setCopied(false);\n\n    try {\n      const response = await fetch(\n        'https://official-joke-api.appspot.com/random_joke'\n      );\n      \n      if (!response.ok) {\n        throw new Error('Failed to fetch joke');\n      }\n      \n      const data: Joke = await response.json();\n      setJoke(data);\n    } catch (err) {\n      setError(err instanceof Error ? err.message : 'An error occurred');\n      setJoke(null);\n    } finally {\n      setLoading(false);\n    }\n  };\n\n  const copyToClipboard = () => {\n    if (!joke) return;\n    \n    const jokeText = `${joke.setup} ${joke.delivery}`;\n    navigator.clipboard.writeText(jokeText);\n    setCopied(true);\n    \n    setTimeout(() => setCopied(false), 2000);\n  };\n\n  return (\n    <div className=\"min-h-screen bg-gradient-to-br from-blue-50 to-indigo-100 flex items-center justify-center p-4\">\n      <div className=\"bg-white rounded-2xl shadow-2xl p-8 max-w-md w-full\">\n        {/* Header */}\n        <div className=\"text-center mb-8\">\n          <h1 className=\"text-4xl font-bold text-indigo-600 mb-2\">😂 Joke Generator</h1>\n          <p className=\"text-gray-600\">Get a random joke to brighten your day!</p>\n        </div>\n\n        {/* Joke Display */}\n        <div className=\"bg-indigo-50 rounded-lg p-6 mb-6 min-h-32 flex flex-col justify-center\">\n          {loading ? (\n            <div className=\"flex items-center justify-center\">\n              <Loader className=\"animate-spin text-indigo-600\" size={32} />\n            </div>\n          ) : error ? (\n            <p className=\"text-red-600 text-center font-medium\">{error}</p>\n          ) : joke ? (\n            <div className=\"space-y-3\">\n              <p className=\"text-lg font-semibold text-gray-800\">{joke.setup}</p>\n              <p className=\"text-lg text-indigo-600 italic\">{joke.delivery}</p>\n            </div>\n          ) : (\n            <p className=\"text-gray-500 text-center\">Click the button below to get started!</p>\n          )}\n        </div>\n\n        {/* Action Buttons */}\n        <div className=\"flex gap-3 mb-4\">\n          <button\n            onClick={fetchJoke}\n            disabled={loading}\n            className=\"flex-1 bg-indigo-600 hover:bg-indigo-700 disabled:bg-gray-400 text-white font-bold py-3 px-4 rounded-lg transition-colors flex items-center justify-center gap-2\"\n          >\n            <RefreshCw size={20} className={loading ? 'animate-spin' : ''} />\n            {loading ? 'Loading...' : 'Get Joke'}\n          </button>\n\n          {joke && (\n            <button\n              onClick={copyToClipboard}\n              className=\"bg-gray-200 hover:bg-gray-300 text-gray-800 font-bold py-3 px-4 rounded-lg transition-colors flex items-center justify-center gap-2\"\n            >\n              {copied ? (\n                <>\n                  <Check size={20} className=\"text-green-600\" />\n                  <span className=\"text-green-600\">Copied!</span>\n                </>\n              ) : (\n                <>\n                  <Copy size={20} />\n                  Copy\n                </>\n              )}\n            </button>\n          )}\n        </div>\n\n        {/* Footer */}\n        <p className=\"text-center text-sm text-gray-500\">\n          Powered by{' '}\n          <a\n            href=\"https://official-joke-api.appspot.com/\"\n            target=\"_blank\"\n            rel=\"noopener noreferrer\"\n            className=\"text-indigo-600 hover:underline font-semibold\"\n          >\n            Official Joke API\n          </a>\n        </p>\n      </div>\n    </div>\n  );\n}\n